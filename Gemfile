source "https://rubygems.org"

# Use the theme gem directly for local dev.
# GitHub Pages uses remote_theme in _config.yml and ignores this gem.
gem "jekyll", "~> 4.3"
gem "minimal-mistakes-jekyll"

group :jekyll_plugins do
  gem "jekyll-paginate"
  gem "jekyll-sitemap"
  gem "jekyll-feed"
  gem "jekyll-include-cache"
end

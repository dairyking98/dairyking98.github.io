let presetsCache=null;export default{async getPresets(){if(presetsCache)return presetsCache;try{const e="?v="+Date.now();console.log("[Preset Loader] Loading presets from JSON file...");const s=await fetch("/assets/json/butterchurn-presets.json"+e);if(console.log("[Preset Loader] Response status:",s.status,s.statusText),!s.ok)throw new Error(`HTTP ${s.status}: ${s.statusText}`);const r=await s.json();if(console.log("[Preset Loader] JSON parsed, checking presets..."),!r||0===Object.keys(r).length)throw new Error("Presets JSON file is empty");const t=Object.keys(r).length;return console.log(`[Preset Loader] Successfully loaded ${t} presets from JSON file`),console.log("[Preset Loader] First few preset names:",Object.keys(r).slice(0,5)),presetsCache=r,presetsCache}catch(e){throw console.error("[Preset Loader] Error loading presets from JSON:",e),console.error("[Preset Loader] Error message:",e.message),console.error("[Preset Loader] Make sure /assets/json/butterchurn-presets.json exists and is accessible"),new Error("Failed to load presets: "+e.message)}}};
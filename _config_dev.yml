# Development configuration override
# Use with: bundle exec jekyll build --config _config.yml,_config_dev.yml

# Disable ImageMagick for faster builds during development
imagemagick:
  enabled: false

# Disable external source fetching (removes network calls)
external_sources: []
# Optionally disable publication badges to skip API calls
# (Comment out if you need these during development)
# enable_publication_badges:
#   altmetric: false
#   dimensions: false
#   inspirehep: false

